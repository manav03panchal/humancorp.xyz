name: Deploy

on:
  push:
    branches: [main]
  repository_dispatch:
    types: [deploy-nexus]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Clone Nexus docs
        run: git clone --depth 1 https://github.com/manav03panchal/nexus.git /tmp/nexus

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Build Nexus docs
        working-directory: /tmp/nexus/website
        run: |
          npm ci
          npm run build

      - name: Prepare site
        run: |
          mkdir -p _site/nexus
          cp index.html _site/
          cp CNAME _site/
          cp -r projects _site/
          cp -r humantime _site/
          cp -r humanboard _site/
          cp -r nexus_l _site/
          touch _site/.nojekyll
          cp -r /tmp/nexus/website/build/* _site/nexus/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
